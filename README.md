# Password manager quick start

Run Front-end

Run the following commands:

```
cd frontend
yarn install
yarn dev
```

Run Back-end

In another shell run the following commands:

```
cd backend
go install
go main.go
```
